-- Выполняется только при ПЕРВОЙ инициализации кластера (пустой PGDATA).
-- Для уже существующего тома нужно один раз выполнить вручную:
--   CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS vector;
